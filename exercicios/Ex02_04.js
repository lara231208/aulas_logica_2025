const prompt = require('prompt-sync')();
let numero =Number(prompt('e um numero ai lindão:'))
if(numero >= 0){
    console.log('seu numero é positivo meu lindão!!')
}else{
        ( numero <= 0)
        console.log('seu numero é negativo meu craque!')
    }